#sh
# The next line is executed by /bin/sh, but not Tcl \
exec tclsh $0

set filelist [glob "*.jpg"]
foreach file $filelist {
  set name [file rootname $file]
  catch { exec convert $file "$name.pgm" } mes
  catch { exec seuil "$name.pgm" 128 "$name.pgm" } mes
}
