#sh
# The next line is executed by /bin/sh, but not Tcl \
exec tclsh $0

set filelist [glob "*.pgm"]
foreach file $filelist {
  set name [file rootname $file]
  catch { exec convert $file "$name.gif" } mes
  catch { exec gifsicle "$name.gif" -O -o "$name.gif" } mes
}


